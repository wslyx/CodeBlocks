#ifndef _PAINT_
#define _PAINT_
/*
#include<windows.h>

void paint(HWND hwnd,HINSTANCE hInstance)
{
    HWND hButton;
    hButton = CreateWindow ( TEXT("button"), BS_PUSHBUTTON, WS_CHILD | WS_VISIBLE | BS_PUSHBUTTON,100, 400,20, 40,hwnd, (HMENU)1,(HINSTANCE)GetWindowLongPtr(hwnd,GWLP_HINSTANCE), NULL) ;

}

*/
#endif _PAINT_
